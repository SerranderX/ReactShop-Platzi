import React from 'react';
import ProductList from '@containers/ProductList';

const Home = () => {
    return (
        <React.Fragment>
            <ProductList />
        </React.Fragment>
    );
};

export default Home;